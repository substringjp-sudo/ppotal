{
  "name": "regionevel",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "type-check": "turbo type-check",
    "deploy": "pnpm build && firebase deploy --only hosting",
    "deploy:preview": "pnpm build && firebase hosting:channel:deploy preview --expires 7d"
  },
  "devDependencies": {
    "firebase-admin": "^13.8.0",
    "firebase-tools": "^14.5.1",
    "tsx": "^4.21.0",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  }
}
