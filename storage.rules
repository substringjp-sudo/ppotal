rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // Tile files (PMTiles / GeoJSON): public read, no client writes
    match /tiles/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
